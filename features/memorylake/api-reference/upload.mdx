---
title: "Upload API"
description: "Upload files to MemoryLake using the REST API"
---

## File Upload

Upload files to a parent folder using multipart form data.

```
POST /drives/items/{parentId}/children
```

### Parameters

<ParamField path="parentId" type="string" required>
Parent folder ID where the file will be uploaded
</ParamField>

### Body (multipart/form-data)

<ParamField body="file" type="file" required>
File to upload
</ParamField>

<ParamField body="name" type="string" required>
File name
</ParamField>

### Example

<RequestExample>
```bash cURL
curl -X POST 'https://api.example.com/api/memorylake/api/v1/drives/items/parent_123/children' \
  -H 'Authorization: Bearer YOUR_API_KEY' \
  -F 'file=@/path/to/file.pdf' \
  -F 'name=file.pdf'
```
</RequestExample>

### Response

Returns the created file item with metadata.

<Tip>
The API automatically handles large files. Simply upload your file and the backend will process it appropriately.
</Tip>

## Best Practices

- Use appropriate error handling for network issues
- Implement retry logic for failed uploads
- Check file size limits based on your plan
- Validate file types before uploading

## Next Steps

<CardGroup cols={2}>
<Card title="Items API" icon="folder" href="/features/memorylake/api-reference/items">
Manage uploaded files
</Card>

<Card title="File Operations" icon="folder-open" href="/features/memorylake/document-management/file-folder-operations">
Learn about file management in the UI
</Card>
</CardGroup>
